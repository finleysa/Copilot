.row
  .large-4.columns.animated.fadeInLeft
    %h1 #{@user.username}'s Profile
    .image
      = image_tag @user.image_url.to_s, class: 'profile-image'

    %h4= @user.email

    /- if current_user == @user

  .large-8.columns.animated.fadeInRight
    -if current_user
      %h2 Welcome #{@user.username}!
      %button.button.button-3d-action.button-large Edit Profile Page
      %fieldset
        - if @my_trips.length > 0
          %h4 #{@user.username}'s Trips
          %table
            %tr
              %th Date
              %th Start Location
              %th End Location
              %th Distance
              %th Est. Cost
              - @my_trips.each do |a|
                -if a
                  /- if Time.now < a.date
                  - b=/\d\d\d\d-\d\d-\d\d/.match(a.date.to_s)
                  %tr
                    %td #{b}
                    %td #{a.location_start}
                    %td #{a.location_end}
                    %td #{a.distance}
                    %td #{a.cost}
                    /- if current_user == a.user_id
                    %td= link_to 'Destroy', a, method: :delete

        - if @user.trips.count > 0
          %h4 Number of Trips Reserved: #{@user.trips.count}
          %table
            %tr
              %th Date
              %th Start Location
              %th End Location
              %th Distance
              %th Est. Cost
              - @user_reservations.each do |a|
                -if a
                  /- if Time.now < a.date
                  - b=/\d\d\d\d-\d\d-\d\d/.match(a.date.to_s)
                  %tr
                    %td #{b}
                    %td #{a.location_start}
                    %td #{a.location_end}
                    %td #{a.distance}
                    %td #{a.cost}
                    %td
                      %a{controller: "reservations", "data-method" => "delete", href: "/reservations/#{a.id}", rel: "nofollow"} Remove



    %h3 About me:
    %h3 What I like to listen to on road trips:
